post_install() {
  if [ ! -f /etc/sensors-monitor/sensors-monitor.conf ]; then
    install -Dm644 /usr/share/sensors-monitor/sensors-monitor.conf /etc/sensors-monitor/sensors-monitor.conf
  fi
}

post_upgrade() {
  # Do nothing – pacman handles .pacnew if config changes
  :
}